<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= @song.name %></title>
  </head>

  <body>
    <h1>
      <%= @song.name %>
    </h1>

    <h2>
      <a href="<%= @song.artist.to_slug %>"><%= @song.artist.name %></a>
    </h2>

    <h3>
      <a href="<%= @song.genre.to_slug %>"><%= @song.genre.name %></a>
    </h3>
  </body>
</html>
